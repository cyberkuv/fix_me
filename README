// after cloaning git clone <link>
// naigate to directory like so
cd fix_me/fix_me
mvn clean install
// then on one terninal window navigate to
cd fix_meRouter
// and type
java -cp target/<get the .jar file> com.wethinkcode.fix_me.Router
// and on another window
cd fix_meBroker
java -cp target/<get the .jar file> com.wethinkcode.fix_me.Broker
// and on the third window
cd fix_meMarket
java -cp target/<get the .jar file> com.wethinkcode.fix_me.Market
